# === Gradle ===
.gradle/
build/
gradle/gradle-daemon-jvm.properties

# === Local config (SDK path + INDIC_API_BASE_URL — NEVER commit) ===
local.properties
.android-sdk/

# === Android Studio / IntelliJ ===
.idea/
*.iml
captures/
.navigation/

# === Native build output (CMake/NDK intermediates, regenerated per machine) ===
app/.cxx/
.externalNativeBuild/

# === APK / bundle outputs ===
*.apk
*.aab
*.ap_
output-metadata.json

# === Signing keys (CI decodes app/release.keystore here; never commit one) ===
*.keystore
*.jks

# === Misc ===
.DS_Store
*.log
*.hprof
app/src/main/cpp/third_party/*
.claude/*
.artifacts/
.vscode/*
.cursor/

# === Kotlin compiler session data ===
.kotlin/
__pycache__/
*.pyc

# === Python virtualenv (BACKEND_SETUP_GCP.md tells contributors to make one here) ===
.venv/
venv/

# === Python test / coverage artifacts ===
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.ruff_cache/

# === Firebase Hosting CLI cache ===
firebase-hosting/.firebase/

# === Generated API Gateway spec (contains the live Cloud Run hostname) ===
backend/gateway/openapi.generated.yaml
# === Local profiling artifacts (large traces / scratch reports; never committed) ===
perf-runs/
scripts/perf/
backend/scripts/perf_usage_report.py
