<?xml version="1.0" encoding="utf-8"?>
<!--
  Benchmark-variant-only overlay. Declares the synthetic-session launcher used by the
  Macrobenchmark viewer profile. This manifest merges ONLY into the release-like
  `benchmark` app variant, so the activity never appears in debug or shipped release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="com.indicvision.semper.benchmark.BenchmarkSeedActivity"
            android:exported="true" />
    </application>
</manifest>
