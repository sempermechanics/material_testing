<?xml version="1.0" encoding="utf-8"?>
<paths>
    <cache-path name="share" path="share/" />
    <!-- FrameImportHelper.COMMITTED_DIR_NAME — video-extracted and imported
         deformed frames, handed off to the wizard via content:// URIs. -->
    <cache-path name="temp_deformed" path="temp_deformed/" />
</paths>
