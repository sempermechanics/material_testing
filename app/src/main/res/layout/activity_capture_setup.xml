<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/surface_muted">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbarCaptureSetup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:title="@string/capture_setup_title"
            app:navigationIcon="@drawable/ic_close" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/capture_setup_intro"
            android:textColor="@color/text_secondary"
            android:textSize="14sp" />

        <!--
            Typed, not dragged: a duration comes from the experiment, and a
            slider spanning ten minutes cannot land on a specific second.
        -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">
            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:labelFor="@+id/etCaptureDuration"
                android:text="@string/capture_duration"
                android:textColor="@color/text_primary"
                android:textStyle="bold" />

            <EditText
                android:id="@+id/etCaptureDuration"
                android:layout_width="96dp"
                android:layout_height="wrap_content"
                android:autofillHints=""
                android:contentDescription="@string/capture_duration_hint"
                android:digits="0123456789:"
                android:gravity="center"
                android:hint="@string/capture_duration_hint"
                android:imeOptions="actionDone"
                android:importantForAutofill="no"
                android:inputType="time"
                android:maxLength="5"
                android:maxLines="1"
                android:textColor="@color/sky_primary"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/tvCaptureDurationRange"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="@string/capture_duration_range"
            android:textColor="@color/text_secondary"
            android:textSize="12sp" />

        <!-- Resolution -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/capture_resolution"
            android:textColor="@color/text_primary"
            android:textStyle="bold" />

        <Spinner
            android:id="@+id/spinnerCaptureResolution"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="6dp" />

        <!--
            Rate is chosen, not requested. Every chip is a rate this device can
            hold at the chosen resolution, so there is nothing here to trim
            later and no "you asked for 150, you got 60" to explain afterwards.
        -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="@string/capture_fps_label"
            android:textColor="@color/text_primary"
            android:textStyle="bold" />

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/chipsCaptureFps"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            app:selectionRequired="true"
            app:singleLine="false"
            app:singleSelection="true" />

        <TextView
            android:id="@+id/tvCaptureEstimate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvCaptureAssurance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/tvCaptureMode"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/sky_on_container"
            android:background="@color/sky_container"
            android:padding="12dp"
            android:textSize="13sp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCaptureContinue"
            style="@style/Widget.Semper.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/capture_continue_test_shot" />
    </LinearLayout>
</ScrollView>
