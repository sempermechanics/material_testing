# HRF format for Baseline Profiles — regenerate via Macrobenchmark on a device:
#   ./gradlew :benchmark:connectedBenchmarkAndroidTest
# Then copy the generated profile into this file (or replace via the
# androidx.baselineprofile plugin when adopted). An empty profile is valid and
# lets profileinstaller ship without blocking release builds.
